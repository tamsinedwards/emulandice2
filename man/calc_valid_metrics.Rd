% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_valid_metrics.R
\name{calc_valid_metrics}
\alias{calc_valid_metrics}
\title{calc_valid_metrics: summarise LOO/TVT predictions and write CSV
(a refactoring by Cursor)}
\usage{
calc_valid_metrics(
  simulated,
  emu_mean,
  emu_sd,
  scenario,
  row_mask = NULL,
  region,
  year,
  csv_file,
  logfile = NULL
)
}
\arguments{
\item{simulated}{Numeric vector of simulator values (one year).}

\item{emu_mean}{Numeric vector of emulator means (same length).}

\item{emu_sd}{Numeric vector of emulator sds (same length).}

\item{scenario}{Scenario labels (same length).}

\item{row_mask}{Logical mask of rows to include (LOO `N_k_index`); default all.}

\item{region}{Character region label for CSV.}

\item{year}{Year label (numeric or character).}

\item{csv_file}{Path to validation CSV (header written if file is new).}

\item{logfile}{Optional build logfile; pointer written when CSV is created.}
}
\value{
Invisibly, a data.frame of rows written (`all` + each scenario).
  `n == 0` groups are skipped. `sd_std_err` is `NA` when `n < 2`.
}
\description{
Compute aggregate and by-scenario validation metrics for one year.
Appends wide rows to `*_validation.csv`. Used for both LOO and TVT.
}
